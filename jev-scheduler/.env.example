# TypeSafe (Jev) API key from https://console.typesafe.ai/keys
TYPESAFE_API_KEY=
TYPESAFE_MODEL=jev-latest
# Set to 1 to force deterministic mock answers (no network). Mock is also used when the key is missing.
JEV_MOCK=
